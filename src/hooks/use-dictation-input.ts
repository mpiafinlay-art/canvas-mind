// Hook de dictado eliminado: implementación anterior se limpia para la nueva lógica centralizada.
// Se exporta un no-op para evitar romper imports existentes mientras se integra la nueva API.

'use client';

export function useDictationInput() {
  // no-op
}

export default useDictationInput;
